name: "snowflakeSmoketest"
version: "0.0.1"
config-version: 2

profile: "smoketest" #"Make sure you a runing DBT from the cssXX repository, not the core.dashboards_store repository."

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
